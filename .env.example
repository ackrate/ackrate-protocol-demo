# LLM providers. At least one key is required to run the research agent.
# If BOTH are set, the agent fails over from the primary to the backup when the
# primary is out of credit or rate-limited, so it never goes dark mid-run.
ANTHROPIC_API_KEY=
OPENAI_API_KEY=

# Optional. Which provider is primary (default: anthropic). Set to "openai" to flip.
LLM_PRIMARY=anthropic

# Optional. Model ids — point these at whatever each key is provisioned for.
ANTHROPIC_MODEL=claude-opus-4-8
ANTHROPIC_MODEL_SUB=claude-sonnet-4-6
OPENAI_MODEL=gpt-5.5
OPENAI_MODEL_SUB=gpt-5.5

# Unlisted Freighter wallet + mandate-aware agent route at /wallet.
# Keep server secrets in Railway variables; never expose them with NEXT_PUBLIC_.
REAPP_WALLET_NETWORK=mainnet
REAPP_CHAT_AGENT_PUBLIC_KEY=
REAPP_CHAT_AGENT_SECRET=
REAPP_CHAT_MERCHANT_PUBLIC_KEY=
REAPP_CHAT_MERCHANT_URL=
REAPP_CHAT_MERCHANT_NAME=Research Source
REAPP_SESSION_SECRET=
REAPP_CHALLENGE_SECRET=
REAPP_APP_ORIGIN=https://reapp.live
REAPP_APP_SOURCE_COMMIT=
REAPP_CHAT_CATALOG_JSON=
DATABASE_URL=

# Mainnet remains fail-closed unless this exact activation phrase is set. The
# verified deployment manifest is pinned in source; the JSON override is only
# for a separately reviewed replacement release.
REAPP_ENABLE_MAINNET=ACTIVATE_VERIFIED_REAPP_MAINNET
REAPP_MAINNET_DEPLOYMENT_MANIFEST_JSON=
